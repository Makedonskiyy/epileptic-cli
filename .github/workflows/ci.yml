name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
        python-version: ["3.10", "3.12"]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: pip install -e ".[dev]"
      - name: Lint
        run: ruff check src tests
      - name: Test
        run: pytest -q
      - name: Smoke test CLI
        run: epileptic --provider demo --model demo -p "smoke" | Select-String -Pattern "demo" -SimpleMatch
        if: runner.os == 'Windows'
      - name: Smoke test CLI
        run: epileptic --provider demo --model demo -p "smoke" | grep -q demo
        if: runner.os != 'Windows'
